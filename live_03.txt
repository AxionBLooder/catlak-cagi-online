list">${ps.slice(0,6).map(p=>`<div><b>Lv ${p.level} • ${h(p.name)}</b><span>${h(p.description)}</span></div>`).join('')}</div></div>`}
function builderBgPreview(name){let b=bgInfo(name);return`<div class="preview"><div class="eyebrow">ARKA PLAN HİKÂYESİ</div><h3>${h(b.name)}</h3><p class="muted">${h(b.region)}</p><p>${h(b.history||'Hikâye girilmemiş.')}</p><p><b>Önerilen Statlar:</b> ${h(list(b.abilities).join(', ')||'-')}</p><p><b>Beceriler:</b> ${h(list(b.skills).join(', ')||'-')}</p><p><b>Araç:</b> ${h(b.tool||'-')}</p><p><b>Başlangıç:</b> ${h(b.equipment||'-')}</p></div>`}
function builderPathPreview(spec,key){let p=st.paths.find(x=>x.species_name===spec&&x.path_key===key);if(!p)return`<div class="preview"><div class="eyebrow">ÖZEL YOL</div><p>Bu ırk için yol seçimi yok veya isteğe bağlı.</p></div>`;return`<div class="preview"><div class="eyebrow">ÖZEL YOL</div><h3>${h(p.name)}</h3><p><b>${h(p.data?.title||'')}</b> ${p.data?.role?'• '+h(p.data.role):''}</p><p>${h(p.data?.summary||'')}</p><div class="mini-list">${list(p.data?.features).map(f=>`<div><b>Lv ${n(f.level)} • ${h(f.name)}</b><span>${h(f.text||'')}</span></div>`).join('')}</div></div>`}
function builder(){let sp=st.speciesRows.length?st.speciesRows.map(x=>speciesInfo(x.name)):st.cat.species.map(x=>speciesInfo(x.name)),bg=st.backgroundRows.length?st.backgroundRows.map(x=>bgInfo(x.name)):st.cat.backgrounds.map(x=>bgInfo(x.name)),cl=st.classRows.length?st.classRows.map(x=>classInfo(x.name)):st.cat.classes.map(x=>classInfo(x.name));let first=sp[0]?.name||'',firstBg=bg[0]?.name||'',paths=st.paths.filter(x=>x.species_name===first);return`<div class="two builder"><section class="card"><div class="eyebrow">KARAKTER OLUŞTURUCU</div><h2>${guest()?'Davetle geldin — karakterini oluştur':'Çatlak Çağı karakterini kur'}</h2>${guest()?'<p class="notice">Hesap açmana gerek yok. Bu tarayıcıdaki misafir oturumun karakterine bağlı kalır.</p>':''}<div class="form"><label class="wide">Karakter adı<input id="bname" maxlength="60" placeholder="Karakter adı"></label><label>Irk<select id="bsp">${sp.map(x=>`<option>${h(x.name)}</option>`).join('')}</select></label><label>Arka plan<select id="bbg">${bg.map(x=>`<option>${h(x.name)}</option>`).join('')}</select></label><label>Sınıf<select id="bcl">${cl.map(x=>`<option>${h(x.name)}</option>`).join('')}</select></label><label>Özel yol<select id="bpath"><option value="">Yok / isteğe bağlı</option>${paths.map(x=>`<option value="${h(x.path_key)}">${h(x.name)}</option>`).join('')}</select></label></div><h3>Başlangıç Statları</h3><div class="effects">${A.map((a,i)=>`<label>${a}<input id="bs-${a}" type="number" min="3" max="30" value="${[15,14,13,12,10,8][i]}"></label>`).join('')}</div><button class="primary widebtn" data-a="create">Karakteri Oluştur</button></section><aside class="stack"><section class="card" id="speciesPreview">${builderSpeciesPreview(first)}</section><section class="card" id="bgPreview">${builderBgPreview(firstBg)}</section><section class="card" id="pathPreview">${builderPathPreview(first,'')}</section></aside></div>`}
function rules(){let mode=st.ruleMode||'species';let sp=(st.speciesRows.length?st.speciesRows.map(x=>speciesInfo(x.name)):st.cat.species.map(x=>speciesInfo(x.name)));return`<section class="card"><div class="section-title"><div><div class="eyebrow">ÇATLAK ÇAĞI KAYNAKLARI</div><h2>${mode==='species'?`${sp.length} Oynanabilir Irk`:`${st.cat.backgrounds.length} Arka Plan`}</h2></div><div class="tabs"><button data-a="rulemode" data-v="species" class="${mode==='species'?'on':''}">Irklar</button><button data-a="rulemode" data-v="background" class="${mode==='background'?'on':''}">Arka Planlar</button></div></div>${mode==='species'?`<div class="grid">${sp.map(s=>`<details><summary><span>${h(s.name)}</span><span class="muted">${s.speed} ft</span></summary>${s.bonus_text?`<p><b>Bonus:</b> ${h(s.bonus_text)}</p>`:''}${speciesPowers(s.name).map(p=>`<div class="power"><small>SEVİYE ${p.level}</small><h3>${h(p.name)}</h3><p>${h(p.description)}</p>${p.detail_note?`<div class="note"><b>DETAY NOTU</b><p>${h(p.detail_note)}</p></div>`:''}</div>`).join('')}</details>`).join('')}</div>`:`<div class="grid">${st.cat.backgrounds.map(x=>{let b=bgInfo(x.name);return`<details><summary><span>${h(b.name)}</span><span class="muted">${h(b.region)}</span></summary><p>${h(b.history)}</p><p><b>Önerilen Statlar:</b> ${h(list(b.abilities).join(', '))}</p><p><b>Beceriler:</b> ${h(list(b.skills).join(', '))}</p><p><b>Araç:</b> ${h(b.tool||'-')}</p><p><b>Başlangıç:</b> ${h(b.equipment||'-')}</p></details>`}).join('')}</div>`}</section>`}
function detail(c){if(!c)return'';let d=der(c),p=charPath(c),s=speciesInfo(c.species_name),b=bgInfo(c.background_name),rows=st.rolls.filter(x=>x.character_id===c.id).slice(0,15);return`<section class="card detail"><div class="section-title"><div><div class="eyebrow">KARAKTER DETAYI</div><h2>${h(c.name)}</h2><p>${h(c.species_name)} • ${h(c.class_name)} ${c.level} • ${h(c.background_name)}</p></div><button data-a="closedetail">Kapat</button></div><div class="vitals"><div class="vital"><span>HP</span><b>${c.hp_current}/${d.hp}</b></div><div class="vital"><span>AC</span><b>${d.ac}</b></div><div class="vital"><span>HIZ</span><b>${d.speed}</b></div><div class="vital"><span>SEVİYE</span><b>${c.level}</b></div></div><div class="stats">${A.map(a=>`<div class="stat static"><b>${a}</b><strong>${d.ds[a]}</strong><small>${sg(md(d.ds[a]))}</small></div>`).join('')}</div><div class="two compact"><div><h3>Irk</h3><p>${h(s.type)} • ${s.speed} ft</p>${s.bonus_text?`<p><b>Bonus:</b> ${h(s.bonus_text)}</p>`:''}<h3>Arka Plan</h3><p>${h(b.history)}</p><p><b>Beceriler:</b> ${h(list(b.skills).join(', '))}</p></div><div><h3>Özel Yol</h3><p>${p?`${h(p.name)} — ${h(p.data?.summary||'')}`:'Yok'}</p><label>GM Karakter Notu<textarea id="char-note">${h(c.data?.gm_note||'')}</textarea></label><button data-a="charnote" data-id="${c.id}">Notu Kaydet</button></div></div>${vampPanel(c,true)}<h3>Irk Güçleri</h3><div class="grid">${powerCards(c)}</div><h3>Envanter</h3>${inventoryFor(c,true)}<h3>Son Zarlar</h3>${rolls(rows,true)}</section>`}
function gmchars(){let dc=st.chars.find(x=>x.id===st.detailCharId);return`<section class="card"><div class="section-title"><div><div class="eyebrow">GM KARAKTER YÖNETİMİ</div><h2>Oyuncular</h2></div><button class="primary" data-a="guestinvite">+ Yeni Oyuncu Daveti</button></div><div class="list">${st.chars.map(c=>{let d=der(c);return`<div class="char"><div><b>${h(c.name)}</b>