name: Deploy Netlify Production
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  guidance:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment note
        run: |
          echo 'Production deploys use a fresh short-lived Netlify deployment authorization.'
          echo 'No reusable Netlify deployment credential is stored in this public repository.'
