(function(){
'use strict';
if(window.__catlakPlayerSheetSupportV2)return;
window.__catlakPlayerSheetSupportV2=true;
const APP=document.querySelector('#app'),S=window.__catlakSupabase;if(!APP||!S)return;
const txt=e=>String(e?.textContent||'').trim();
const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const isPlayer=()=>txt(APP.querySelector('.role'))!=='GM';
const isSheet=()=>isPlayer()&&APP.querySelector('.nav button.on[data-tab]')?.dataset.tab==='sheet'&&APP.querySelector('main')?.dataset.ccHardSheet!=='1';
const charId=stack=>stack?.querySelector('section.hero [data-a="hp"][data-id]')?.dataset.id||'';
const slots=['main_weapon','off_weapon','armor','accessory_1','accessory_2'];
const slotName=k=>({main_weapon:'1. Silah',off_weapon:'2. Silah',armor:'Zırh',accessory_1:'Aksesuar 1',accessory_2:'Aksesuar 2'})[k]||k;
let busy=false,pending=false,timer=0,lastSig='';

if(!document.querySelector('#pssr-style')){const s=document.createElement('style');s.id='pssr-style';s.textContent=`
.psv4-sheet [data-gmt-player-panel]{padding:12px!important;border-color:#394c5c!important;background:#09131e!important}.psv4-sheet [data-gmt-player-panel] .gmt-player-two{display:flex!important;flex-direction:column!important;gap:9px!important}.psv4-sheet [data-gmt-player-panel] .gmt-player-two>div{padding:10px;border:1px solid #263b4c;border-radius:11px;background:#07111b}.psv4-sheet [data-gmt-player-panel] .gmt-slot{display:grid;grid-template-columns:92px minmax(0,1fr);gap:8px;padding:6px 0;border-bottom:1px solid var(--line);font-size:.8rem}.psv4-sheet [data-gmt-player-panel] .gmt-slot:last-child{border-bottom:0}.psv4-sheet [data-gmt-player-panel] .gmt-slot b{color:var(--gold)}.pssr-status-pill{display:inline-flex;border:1px solid #4b6174;border-radius:999px;padding:4px 8px;margin:3px 4px 3px 0;font-size:.76rem;background:#0b1824}.pssr-status-pill b{margin-right:3px;color:#ffd08a}
`;document.head.appendChild(s)}

function ensurePanel(stack){if(!stack)return null;let panel=stack.querySelector('[data-gmt-player-panel]');if(!panel){panel=document.createElement('section');panel.className='card gmt-player-panel';panel.dataset.gmtPlayerPanel=charId(stack)||'player';panel.innerHTML='<div class="gmt-player-two"><div data-gmt-status-pane><div class="eyebrow">AKTİF DURUMLAR</div><h2>Durum Etkileri</h2><div data-gmt-status-list><p class="muted">Aktif durum etkisi yok.</p></div></div><div data-gmt-equip-pane><div class="eyebrow">TAKILI TEÇHİZAT</div><h2>Slotlar</h2><div data-pssr-slots></div></div></div>';stack.appendChild(panel)}let holder=panel.querySelector('[data-pssr-slots]');if(!holder){holder=document.createElement('div');holder.dataset.pssrSlots='1';panel.querySelector('[data-gmt-equip-pane]')?.appendChild(holder)}for(const key of slots){let row=holder.querySelector(`[data-gmt-slot="${key}"]`);if(!row){row=document.createElement('div');row.className='gmt-slot';row.dataset.gmtSlot=key;row.innerHTML='<b></b><span class="muted">Boş</span>';holder.appendChild(row)}row.querySelector('b').textContent=slotName(key)}return panel}
async function load(ids){const [kr,vr,ir]=await Promise.all([S.from('catlak_character_conditions').select('id,character_id,name,remaining_rounds,note,active').in('character_id',ids).eq('active',true).order('created_at',{ascending:true}),S.from('catlak_inventory').select('id,character_id,item_id,equipped,equipped_slot,quantity').in('character_id',ids).eq('equipped',true).order('granted_at',{ascending:true}),S.from('catlak_items').select('id,name,is_active')]);for(const r of [kr,vr,ir])if(r.error)throw r.error;return{conditions:kr.data||[],inventory:vr.data||[],items:ir.data||[]}}
function paintStack(stack,data){const cid=charId(stack);if(!cid)return;const panel=ensurePanel(stack);if(!panel)return;const status=panel.querySelector('[data-gmt-status-list]'),conds=data.conditions.filter(x=>String(x.character_id)===String(cid));if(status)status.innerHTML=conds.length?conds.map(x=>`<span class="pssr-status-pill"><b>${esc(x.name||'Durum')}</b>${x.remaining_rounds==null?'':' • '+Number(x.remaining_rounds)+' round'}${x.note?' • '+esc(x.note):''}</span>`).join(''):'<p class="muted">Aktif durum etkisi yok.</p>';const itemMap=new Map(data.items.map(i=>[String(i.id),i])),rows=data.inventory.filter(x=>String(x.character_id)===String(cid)&&x.equipped);for(const key of slots){const row=panel.querySelector(`[data-gmt-slot="${key}"]`),hit=rows.find(x=>String(x.equipped_slot||'')===key),item=hit&&itemMap.get(String(hit.item_id)),span=row?.querySelector(':scope > span');if(!span)continue;const ok=item&&item.is_active!==false;span.textContent=ok?item.name:'Boş';span.classList.toggle('muted',!ok)}}
async function refresh(force=false){if(!isSheet())return false;if(busy){pending=true;return false}const stacks=[...APP.querySelectorAll('main .cc-character-stack')],ids=[...new Set(stacks.map(charId).filter(Boolean))];if(!ids.length)return false;stacks.forEach(ensurePanel);busy=true;try{const data=await load(ids);if(!isSheet())return false;const sig=JSON.stringify([data.conditions.map(x=>[x.id,x.name,x.remaining_rounds,x.note]),data.inventory.map(x=>[x.id,x.item_id,x.equipped_slot]),data.items.map(x=>[x.id,x.name,x.is_active])]);if(force||sig!==lastSig){lastSig=sig;stacks.forEach(st=>paintStack(st,data));try{window.__catlakPlayerSheetTest?.apply?.()}catch(_){}try{window.__catlakPlayerSheetEquipmentAbilitiesTest?.paint?.(false)}catch(_){}}return true}catch(e){console.warn('CATLAK_PLAYER_SHEET_SUPPORT',e);return false}finally{busy=false;if(pending){pending=false;schedule(true,0)}}}
function schedule(force=false,ms=0){clearTimeout(timer);timer=setTimeout(()=>refresh(force),ms)}
new MutationObserver(()=>{if(isSheet())schedule(false,10)}).observe(APP,{childList:true,subtree:true});
S.channel('cc-player-sheet-support-v2').on('postgres_changes',{event:'*',schema:'public',table:'catlak_inventory'},()=>schedule(true,0)).on('postgres_changes',{event:'*',schema:'public',table:'catlak_items'},()=>schedule(true,0)).on('postgres_changes',{event:'*',schema:'public',table:'catlak_character_conditions'},()=>schedule(true,0)).subscribe();
setInterval(()=>{if(isSheet())refresh(false)},700);setTimeout(()=>schedule(true,0),0);
window.__catlakPlayerSheetSupport={refresh,schedule,ensurePanel};
})();