b><p class="muted">${h(c.species_name)} • ${h(c.class_name)} ${c.level} • ${c.owner_id?'Oyuncuya bağlı':'Sahipsiz'}</p><span>HP ${c.hp_current}/${d.hp} • AC ${d.ac} • Hız ${d.speed}</span></div><div class="actions"><button data-a="detailchar" data-id="${c.id}">Detay</button><button data-a="level" data-id="${c.id}" data-d="-1">Lv −</button><button data-a="level" data-id="${c.id}" data-d="1">Lv +</button><button data-a="hp" data-id="${c.id}" data-d="-1">HP −</button><button data-a="hp" data-id="${c.id}" data-d="1">HP +</button>${!c.owner_id?`<button class="primary" data-a="claimcode" data-id="${c.id}">Hazır Karakter Daveti</button>`:''}<button class="danger" data-a="chardelete" data-id="${c.id}">Sil</button></div></div>`}).join('')}</div></section>${dc?detail(dc):''}`}
function gmhome(){return`<section class="card hero"><div><div class="eyebrow">GM CANLI MASA</div><h1>Çatlak Çağı</h1><p>${st.chars.length} karakter • ${st.items.length} eşya • canlı zar ve envanter</p><div class="actions"><button class="primary" data-a="guestinvite">Yeni Oyuncu Davet Linki Oluştur</button></div></div><div class="vitals"><div class="vital"><span>IRK</span><b>${st.speciesRows.length||19}</b></div><div class="vital"><span>ARKA PLAN</span><b>${st.backgroundRows.length||22}</b></div><div class="vital"><span>CANLI</span><b>●</b></div></div></section>${gmchars()}<section class="card"><div class="eyebrow">SON ZARLAR</div>${rolls(st.rolls.slice(0,12),true)}</section>`}
function powerEditor(spec){let ps=speciesPowers(spec);return`<div class="stack">${ps.map((p,i)=>`<article class="power editpower"><div class="form"><label>Seviye<input id="pw-lv-${i}" type="number" min="1" max="20" value="${p.level}"></label><label>Güç adı<input id="pw-name-${i}" value="${h(p.name)}"></label><label class="wide">Açıklama<textarea id="pw-desc-${i}">${h(p.description)}</textarea></label><label class="wide">GM Detay Notu<textarea id="pw-note-${i}">${h(p.detail_note||'')}</textarea></label></div><div class="actions"><button data-a="powersave" data-spec="${h(spec)}" data-i="${i}" data-oldlv="${p.level}" data-oldname="${h(p.name)}">Gücü Kaydet</button><button class="danger" data-a="powerdelete" data-spec="${h(spec)}" data-lv="${p.level}" data-name="${h(p.name)}" data-canonical="${p.canonical?'1':'0'}">Gücü Kaldır</button></div></article>`).join('')}<article class="power"><h3>Yeni Irk Gücü</h3><div class="form"><label>Seviye<input id="newpw-lv" type="number" min="1" max="20" value="1"></label><label>Ad<input id="newpw-name"></label><label class="wide">Açıklama<textarea id="newpw-desc"></textarea></label><label class="wide">GM Detay Notu<textarea id="newpw-note"></textarea></label></div><button class="primary" data-a="poweradd" data-spec="${h(spec)}">Güç Ekle</button></article></div>`}
function pathEditor(spec){let paths=st.paths.filter(x=>x.species_name===spec);return`<div class="stack">${paths.map((p,pi)=>`<article class="path-edit"><div class="form"><label>Yol adı<input id="path-name-${pi}" value="${h(p.name)}"></label><label>Başlık<input id="path-title-${pi}" value="${h(p.data?.title||'')}"></label><label>Rol<input id="path-role-${pi}" value="${h(p.data?.role||'')}"></label><label class="wide">Özet<textarea id="path-summary-${pi}">${h(p.data?.summary||'')}</textarea></label><label class="wide">GM Yol Notu<textarea id="path-note-${pi}">${h(p.detail_note||'')}</textarea></label></div><h4>Açılan Yol Güçleri</h4>${list(p.data?.features).map((f,fi)=>`<div class="feature-row"><input id="pf-lv-${pi}-${fi}" type="number" min="1" max="20" value="${n(f.level)}"><input id="pf-name-${pi}-${fi}" value="${h(f.name)}"><textarea id="pf-text-${pi}-${fi}">${h(f.text||'')}</textarea><button class="danger small" data-a="pathfeaturedelete" data-path="${h(p.path_key)}" data-spec="${h(spec)}" data-i="${fi}">Sil</button></div>`).join('')}<div class="actions"><button data-a="pathsave" data-spec="${h(spec)}" data-path="${h(p.path_key)}" data-pi="${pi}">Yolu ve Güçleri Kaydet</button><button data-a="pathfeatureadd" data-spec="${h(spec)}" data-path="${h(p.path_key)}">+ Güç Ekle</button><button class="danger" data-a="pathdelete" data-spec="${h(spec)}" data-path="${h(p.path_key)}">Yolu Sil</button></div></article>`).join('')}<article class="path-edit"><h3>Yeni Özel Yol</h3><div class="form"><label>Yol adı<input id="newpath-name"></label><label>Başlık<input id="newpath-title"></label><label>Rol<input id="newpath-role"></label><label class="wide">Özet<textarea id="newpath-summary"></textarea></label></div><button class="primary" data-a="pathadd" data-spec="${h(spec)}">Yol Oluştur</button></article></div>`}
function raceWorkshop(){let names=st.speciesRows.map(x=>x.name),sel=st.speciesEdit&&names.includes(st.speciesEdit)?st.speciesEdit:names[0],s=sel?speciesInfo(sel):null,custom=!!s?.data?.custom;return`<div class="two race-work"><section class="card"><div class="section-title"><div><div class="eyebrow">GM IRK ATÖLYESİ</div><h2>${s?`Irkı Düzenle: ${h(s.name)}`:'Custom Irk Oluştur'}</h2></div><select id="sedit"><option value="">+ Yeni Custom Irk</option>${names.map(x=>`<option ${x===sel?'selected':''}>${h(x)}</option>`).join('')}</select></div><div class="form"><label>Irk adı<input id="sname" value="${h(s?.name||'')}" ${s?'disabled':''}></label><label>Tür<input id="stype" value="${h(s?.type||'Humanoid')}"></label><label>Hız (ft)<input id="sspeed" type="number" min="0" max="200" value="${n(s?.speed||30)}"></label><label class="wide">Irk bonusu / özel kural<textarea id="sbonus">${h(s?.bonus_text||'')}</textarea></label><label class="wide">Irk açıklaması<textarea id="sdesc">${h(s?.description||'')}</textarea></label></div><div class="actions"><button class="primary" data-a="speciessave" data-existing="${s?'1':'0'}">${s?'Irk Ayarlarını Kaydet':'Custom Irk Oluştur'}</button>${custom?`<button class="danger" data-a="speciesdelete" data-spec="${h(s.name)}">Custom Irkı Sil</button>`:''}</div><div class="note"><b>CANLI ETKİ</b><p>Hız değişikliği mevcut karakterlerin kağıdına da yansır. Güç ve yol değişiklikleri seviyeye göre otomatik görünür.</p></div></section><aside class="card"><div class="eyebrow">ÖNİZLEME</div>${s?builderSpeciesPreview(s.name):'<p>Yeni ırk bilgilerini doldur.</p>'}</aside></div>${s?`<section class="card"><div class="eyebrow">IRK GÜÇLERİ • CUSTOM DÜZENLEME</div><h2>${h(s.name)} Güçleri</h2>${powerEditor(s.name)}</section><section class="card"><div class="eyebrow">ÖZEL YOLLAR • CUSTOM DÜZENLEME</div><h2>${h(s.name)} Yolları</h2>${pathEditor(s.name)}</sect