,role:document.querySelector('#path-role-'+pi).value,summary:document.querySelector('#path-summary-'+pi).value,features},};await savePath(key,spec,data,document.querySelector('#path-note-'+pi).value);msg('Yol ve açılan güçler kaydedildi.');await refresh()}
if(a==='pathfeatureadd'){let p=st.paths.find(x=>x.species_name===b.dataset.spec&&x.path_key===b.dataset.path),data={...(p.data||{}),features:[...(p.data?.features||[]),{level:3,name:'Yeni Güç',text:''}]},{error}=await S.from('catlak_special_paths').update({data}).eq('species_name',b.dataset.spec).eq('path_key',b.dataset.path);if(error)throw error;await refresh()}
if(a==='pathfeaturedelete'){let p=st.paths.find(x=>x.species_name===b.dataset.spec&&x.path_key===b.dataset.path),features=[...(p.data?.features||[])];features.splice(n(b.dataset.i),1);let{error}=await S.from('catlak_special_paths').update({data:{...(p.data||{}),features}}).eq('species_name',b.dataset.spec).eq('path_key',b.dataset.path);if(error)throw error;msg('Yol gücü silindi.');await refresh()}
if(a==='pathadd'){let spec=b.dataset.spec,name=document.querySelector('#newpath-name').value.trim();if(!name)throw new Error('Yol adı gerekli');let key=slugify(name)+'-'+Date.now().toString(36).slice(-4),data={custom:true,title:document.querySelector('#newpath-title').value,role:document.querySelector('#newpath-role').value,summary:document.querySelector('#newpath-summary').value,features:[]},sort=Math.max(0,...st.paths.map(x=>n(x.sort_order)))+1,{error}=await S.from('catlak_special_paths').insert({species_name:spec,path_key:key,name,data,sort_order:sort});if(error)throw error;msg('Yeni özel yol oluşturuldu.');await refresh()}
if(a==='pathdelete'){if(!confirm(`${b.dataset.path} yolu silinsin mi?`))return;let{error}=await S.from('catlak_special_paths').delete().eq('species_name',b.dataset.spec).eq('path_key',b.dataset.path);if(error)throw error;msg('Özel yol silindi.');await refresh()}
}catch(x){err(x)}});
function catlakReleaseBootCover(){try{window.__catlakReleaseStartCover?.()}catch{}try{document.documentElement.classList.remove('cc-player-critical-pending');const cover=document.getElementById('cc-start-cover');if(cover){cover.classList.add('cc-done');setTimeout(()=>cover.remove(),180)}}catch{}}
async function catlakBootJoinedCharacter(ses){
  const uid=ses?.user?.id;if(!uid)return false;
  st.ses=ses;st.role='player';st.tab='sheet';
  const waits=[0,80,180,360,700,1200];
  let last=0;
  for(const ms of waits){
    if(ms>last)await new Promise(r=>setTimeout(r,ms-last));last=ms;
    try{if(await catlakFastPlayerBoot(uid)){catlakReleaseBootCover();return true}}catch(e){console.warn('CATLAK_JOIN_FAST_BOOT',e)}
  }
  try{render()}catch(_){}
  catlakReleaseBootCover();
  return false;
}
const catReady=catload();let joinCode=new URLSearchParams(window.location.search).get('join');let session=null,authBootTimedOut=false;const authBootResult=await Promise.race([S.auth.getSession().then(x=>({ok:true,x})).catch(error=>({ok:false,error})),new Promise(resolve=>setTimeout(()=>resolve({timeout:true}),4500))]);if(authBootResult?.timeout){authBootTimedOut=true;console.warn('CATLAK_AUTH_BOOT_TIMEOUT');}else if(authBootResult?.ok){session=authBootResult.x?.data?.session||null;}else{console.warn('CATLAK_AUTH_BOOT_ERROR',authBootResult?.error);}
if(joinCode){try{let joinMode='sheet';if(session){let{data:r}=await S.from('catlak_roles').select('role').eq('user_id',session.user.id).maybeSingle();if(r?.role==='gm')throw new Error('GM oturumunda oyuncu davet linki kullanılamaz. Linki oyuncuya gönder.')}if(session&&/^GUEST-/i.test(joinCode)){let{error}=await S.rpc('catlak_consume_guest_invite',{p_code:joinCode});if(error)throw error;joinMode='builder';st.tab='builder';msg('Davet kabul edildi. Karakterini oluştur.')}else{let ctrl=new AbortController(),kill=setTimeout(()=>ctrl.abort(),12000),resp;try{resp=await fetch('https://ygbereitlhjvoqgqlwfe.supabase.co/functions/v1/catlak-guest-join',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:joinCode}),signal:ctrl.signal})}finally{clearTimeout(kill)}let j=await resp.json();if(!resp.ok)throw new Error(j.detail||j.error||'Davet açılamadı');let{error}=await S.auth.setSession({access_token:j.access_token,refresh_token:j.refresh_token});if(error)throw error;authBootTimedOut=false;joinMode=j.mode==='builder'?'builder':'sheet';st.tab=joinMode;session=(await S.auth.getSession()).data.session;msg(joinMode==='builder'?'Davetle katıldın. Karakterini oluştur.':`${j.character_name} karakterine katıldın.`);setTimeout(()=>T?.classList.add('hidden'),1500)}window.__catlakJoinHandledByCore=true;if(joinMode==='sheet')await catlakBootJoinedCharacter(session);else{st.ses=session;catlakReleaseBootCover()}catlakReleaseBootCover();history.replaceState({},'',window.location.pathname)}catch(e){window.__catlakJoinCoreError=e?.message||String(e);err(e)}}
st.ses=null;let authRefreshTimer=null,catlakRefreshRun=null,catlakRefreshAgain=false,catlakTableReady=false,catlakBootUser=null,catlakBootSeq=0;
function catlakShowLoading(){if(app.querySelector('[data-cc-auth-loading]'))return;app.innerHTML='<main class="auth" data-cc-auth-loading="1"><section class="card"><div class="mark bigmark">◇</div><div class="eyebrow">OTURUM AÇILDI</div><h2>Masa hazırlanıyor…</h2><p class="muted">Karakter ve oyun verileri yükleniyor.</p></section></main>'}
function catlakShowRefreshError(e,uid){if(st.ses?.user?.id!==uid)return;catlakTableReady=false;app.innerHTML='<main class="auth" data-cc-refresh-error="1"><section class="card"><div class="mark bigmark">◇</div><div class="eyebrow">BAĞLANTI KURULDU</div><h2>Oyun verileri yüklenemedi</h2><p class="muted">Oturum açık. Masa verileri zamanında alınamadı. Tekrar deneyebilir veya çıkış yapabilirsin.</p><button class="primary widebtn" id="catlak-refresh-retry">Masa Verilerini Yeniden Yükle</button><button class="widebtn" id="catlak-refresh-logout">Çıkış Yap</button></section></main>';document.querySelector('#catlak-refresh-retry')?.addEventListener('click',()=>{if(st.ses?.user?.id===uid)setTimeout(()=>catlakSafeRefresh('retry'),80)});document.querySelector('#catlak-refresh-logout')?.addEventListener('click',()=>S.auth.signOut());msg(e?.message||'Oyun verileri yüklenemedi.')}
async function catlakFastPlayerBoot(uid){try{if(!st.ses?.user||st.ses.user.id!==uid)return false;let[rr,cr]=await Promise.all([S.from('catlak_roles').select('role').eq('user_id',uid).maybeSingle(),S.from('catlak_characters').select('*').eq('owner_id',uid).order('created_at')]);if(rr.error||cr.error)return false;let role=rr.data?.role||'player',rows=cr.data||[];if(role==='gm'||!rows.length||st.ses?.user?.id!==uid)return false;st.role='player';st.chars=rows;st.tab='sheet';render();catlakReleaseBootCover();window.dispatchEvent(new CustomEvent('catlak:player-fast-ready',{detail:{user_id:uid,count:rows.length}}));return true}catch(e){console.warn('CATLAK_FAST_PLAYER_BOOT',e);return false}}
async function catlakSafeRefresh(reason='manual'){if(!st.ses?.user){catlakTableReady=false;render();return}if(catlakRefreshRun){catlakRefreshAgain=true;return catlakRefreshRun}const uid=st.ses.user.id,seq=++catlakBootSeq;if(!catlakTableReady)catlakShowLoading();let fastTimer=setTimeout(()=>{if(!catlakTableReady&&st.ses?.user?.id===uid)catlakFastPlayerBoot(uid)},700);catlakRefreshRun=(async()=>{try{await Promise.race([refresh(),new Promise((_,reject)=>setTimeout(()=>reject(new Error('Oyun verileri zaman aşımına uğradı. Lütfen tekrar dene.')),10000))]);if(st.ses?.user?.id===uid&&seq===catlakBootSeq){catlakTableReady=true;catlakBootUser=uid}}catch(e){console.error('CATLAK_REFRESH_ERROR',reason,e);if(st.ses?.user?.id===uid&&seq===catlakBootSeq){let recovered=await catlakFastPlayerBoot(uid);if(!recovered)catlakShowRefreshError(e,uid)}}finally{clearTimeout(fastTimer)}})();try{return await catlakRefreshRun}finally{catlakRefreshRun=null;if(catlakRefreshAgain&&st.ses?.user?.id===uid){catlakRefreshAgain=false;setTimeout(()=>catlakSafeRefresh('queued'),120)}else catlakRefreshAgain=false}}
function catlakBeginSession(s,ev='AUTH'){clearTimeout(authRefreshTimer);if(!s){st.ses=null;catlakTableReady=false;catlakBootUser=null;catlakBootSeq++;render();return}const uid=s.user.id,changed=st.ses?.user?.id!==uid;st.ses=s;if(changed){catlakTableReady=false;catlakBootUser=null;catlakBootSeq++}if(catlakTableReady&&catlakBootUser===uid)return;if(catlakRefreshRun){catlakRefreshAgain=true;return}catlakShowLoading();authRefreshTimer=setTimeout(()=>{if(st.ses?.user?.id===uid&&!catlakTableReady&&!catlakRefreshRun)catlakSafeRefresh(ev)},80)}
S.auth.onAuthStateChange((ev,s)=>{setTimeout(()=>catlakBeginSession(s,ev),0)});
if(authBootTimedOut){st.ses=null;render();const card=app.querySelector('.auth .card');if(card){const recovery=document.createElement('div');recovery.className='notice';recovery.innerHTML='<b>Oturum kontrolü yanıt vermedi.</b><p>Tarayıcıda eski veya kilitlenmiş bir oturum kalmış olabilir. Aşağıdaki düğme yalnızca bu siteye ait yerel oturum kaydını temizleyip sayfayı yeniden açar.</p><button type="button" id="catlak-auth-recover">Oturumu Temizle ve Yeniden Aç</button>';card.appendChild(recovery);const recover=document.querySelector('#catlak-auth-recover');if(recover)recover.addEventListener('click',()=>{try{Object.keys(localStorage).filter(k=>/^sb-.*-auth-token$/i.test(k)).forEach(k=>localStorage.removeItem(k))}catch{}location.reload()});}msg('Oturum kontrolü zaman aşımına uğradı.')}else if(session)catlakBeginSession(session,'BOOT');else render();catReady.catch(e=>console.warn('CATLAK_CATALOG_BACKGROUND',e));
let liveRefreshTimer=null;const liveRefresh=()=>{if(!catlakTableReady||!st.ses?.user)return;clearTimeout(liveRefreshTimer);liveRefreshTimer=setTimeout(()=>{if(catlakTableReady&&st.ses?.user)catlakSafeRefresh('realtime')},250)};S.channel('catlak-live').on('postgres_changes',{event:'*',schema:'public',table:'catlak_rolls'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_inventory'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_characters'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_items'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_species_powers'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_species'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_backgrounds'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_classes'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_special_paths'},liveRefresh).subscribe();