,role:document.querySelector('#path-role-'+pi).value,summary:document.querySelector('#path-summary-'+pi).value,features},};await savePath(key,spec,data,document.querySelector('#path-note-'+pi).value);msg('Yol ve açılan güçler kaydedildi.');await refresh()}
if(a==='pathfeatureadd'){let p=st.paths.find(x=>x.species_name===b.dataset.spec&&x.path_key===b.dataset.path),data={...(p.data||{}),features:[...(p.data?.features||[]),{level:3,name:'Yeni Güç',text:''}]},{error}=await S.from('catlak_special_paths').update({data}).eq('species_name',b.dataset.spec).eq('path_key',b.dataset.path);if(error)throw error;await refresh()}
if(a==='pathfeaturedelete'){let p=st.paths.find(x=>x.species_name===b.dataset.spec&&x.path_key===b.dataset.path),features=[...(p.data?.features||[])];features.splice(n(b.dataset.i),1);let{error}=await S.from('catlak_special_paths').update({data:{...(p.data||{}),features}}).eq('species_name',b.dataset.spec).eq('path_key',b.dataset.path);if(error)throw error;msg('Yol gücü silindi.');await refresh()}
if(a==='pathadd'){let spec=b.dataset.spec,name=document.querySelector('#newpath-name').value.trim();if(!name)throw new Error('Yol adı gerekli');let key=slugify(name)+'-'+Date.now().toString(36).slice(-4),data={custom:true,title:document.querySelector('#newpath-title').value,role:document.querySelector('#newpath-role').value,summary:document.querySelector('#newpath-summary').value,features:[]},sort=Math.max(0,...st.paths.map(x=>n(x.sort_order)))+1,{error}=await S.from('catlak_special_paths').insert({species_name:spec,path_key:key,name,data,sort_order:sort});if(error)throw error;msg('Yeni özel yol oluşturuldu.');await refresh()}
if(a==='pathdelete'){if(!confirm(`${b.dataset.path} yolu silinsin mi?`))return;let{error}=await S.from('catlak_special_paths').delete().eq('species_name',b.dataset.spec).eq('path_key',b.dataset.path);if(error)throw error;msg('Özel yol silindi.');await refresh()}
}catch(x){err(x)}});
const catReady=catload();let joinCode=new URLSearchParams(window.location.search).get('join');let session=null,authBootTimedOut=false;const authBootResult=await Promise.race([S.auth.getSession().then(x=>({ok:true,x})).catch(error=>({ok:false,error})),new Promise(resolve=>setTimeout(()=>resolve({timeout:true}),4500))]);if(authBootResult?.timeout){authBootTimedOut=true;console.warn('CATLAK_AUTH_BOOT_TIMEOUT');}else if(authBootResult?.ok){session=authBootResult.x?.data?.session||null;}else{console.warn('CATLAK_AUTH_BOOT_ERROR',authBootResult?.error);}
if(joinCode&&!authBootTimedOut){try{if(session){let{data:r}=await S.from('catlak_roles').select('role').eq('user_id',session.user.id).maybeSingle();if(r?.role==='gm')throw new Error('GM oturumunda oyuncu davet linki kullanılamaz. Linki oyuncuya gönder.');if(/^GUEST-/i.test(joinCode)){let{error}=await S.rpc('catlak_consume_guest_invite',{p_code:joinCode});if(error)throw error;st.tab='builder';msg('Davet kabul edildi. Karakterini oluştur.')}else{let{error}=await S.rpc('catlak_claim_character',{p_code:joinCode});if(error)throw error;st.tab='sheet';msg('Hazır karakter hesabına bağlandı.')}}else{let resp=await fetch('https://ygbereitlhjvoqgqlwfe.supabase.co/functions/v1/catlak-guest-join',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:joinCode})}),j=await resp.json();if(!resp.ok)throw new Error(j.detail||j.error||'Davet açılamadı');let{error}=await S.auth.setSession({access_token:j.access_token,refresh_token:j.refresh_token});if(error)throw error;st.tab=j.mode==='builder'?'builder':'sheet';session=(await S.auth.getSession()).data.session;msg(j.mode==='builder'?'Davetle katıldın. Karakterini oluştur.':`${j.character_name} karakterine katıldın.`)}history.replaceState({},'',window.location.pathname)}catch(e){err(e)}}
st.ses=session;let authRefreshTimer=null,catlakRefreshRun=null,catlakRefreshAgain=false;
async function catlakSafeRefresh(){if(!st.ses?.user){render();return}if(catlakRefreshRun){catlakRefreshAgain=true;return catlakRefreshRun}const uid=st.ses.user.id;catlakRefreshRun=(async()=>{try{await Promise.race([refresh(),new Promise((_,reject)=>setTimeout(()=>reject(new Error('Oyun verileri zaman aşımına uğradı. Lütfen tekrar dene.')),12000))])}catch(e){console.error('CATLAK_REFRESH_ERROR',e);if(st.ses?.user?.id===uid){app.innerHTML='<main class="auth"><section class="card"><div class="mark bigmark">◇</div><div class="eyebrow">BAĞLANTI KURULDU</div><h2>Oyun verileri yüklenemedi</h2><p class="muted">Oturum açıldı fakat masa verileri zamanında alınamadı. Yeniden denemek için aşağıdaki düğmeyi kullan.</p><button class="primary widebtn" id="catlak-refresh-retry">Masa Verilerini Yeniden Yükle</button><button class="widebtn" id="catlak-refresh-logout">Çıkış Yap</button></section></main>';document.querySelector('#catlak-refresh-retry')?.addEventListener('click',()=>setTimeout(()=>catlakSafeRefresh(),0));document.querySelector('#catlak-refresh-logout')?.addEventListener('click',()=>S.auth.signOut());msg(e.message||'Oyun verileri yüklenemedi.')}}})();try{return await catlakRefreshRun}finally{catlakRefreshRun=null;if(catlakRefreshAgain&&st.ses?.user){catlakRefreshAgain=false;setTimeout(()=>catlakSafeRefresh(),150)}}}
S.auth.onAuthStateChange((ev,s)=>{st.ses=s;if(!s){clearTimeout(authRefreshTimer);render();return}if((ev==='SIGNED_IN'||ev==='INITIAL_SESSION'||ev==='TOKEN_REFRESHED')&&app.querySelector('.auth')){app.innerHTML='<main class="auth"><section class="card"><div class="mark bigmark">◇</div><div class="eyebrow">OTURUM AÇILDI</div><h2>Masa hazırlanıyor…</h2><p class="muted">Karakter ve oyun verileri yükleniyor.</p></section></main>';clearTimeout(authRefreshTimer);authRefreshTimer=setTimeout(()=>{if(st.ses?.user?.id===s.user.id)catlakSafeRefresh()},0)}});
if(authBootTimedOut){st.ses=null;render();const card=app.querySelector('.auth .card');if(card){const recovery=document.createElement('div');recovery.className='notice';recovery.innerHTML='<b>Oturum kontrolü yanıt vermedi.</b><p>Tarayıcıda eski veya kilitlenmiş bir oturum kalmış olabilir. Aşağıdaki düğme yalnızca bu siteye ait yerel oturum kaydını temizleyip sayfayı yeniden açar.</p><button type="button" id="catlak-auth-recover">Oturumu Temizle ve Yeniden Aç</button>';card.appendChild(recovery);const recover=document.querySelector('#catlak-auth-recover');if(recover)recover.addEventListener('click',()=>{try{Object.keys(localStorage).filter(k=>/^sb-.*-auth-token$/i.test(k)).forEach(k=>localStorage.removeItem(k))}catch{}location.reload()});}msg('Oturum kontrolü zaman aşımına uğradı.');}else if(session)await catlakSafeRefresh();else render();catReady.then(()=>{if(st.ses)catlakSafeRefresh();else if(!authBootTimedOut)render()}).catch(()=>{});
let liveRefreshTimer=null;const liveRefresh=()=>{clearTimeout(liveRefreshTimer);liveRefreshTimer=setTimeout(()=>{if(st.ses)catlakSafeRefresh()},180)};S.channel('catlak-live').on('postgres_changes',{event:'*',schema:'public',table:'catlak_rolls'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_inventory'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_characters'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_items'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_species_powers'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_species'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_backgrounds'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_classes'},liveRefresh).on('postgres_changes',{event:'*',schema:'public',table:'catlak_special_paths'},liveRefresh).subscribe();