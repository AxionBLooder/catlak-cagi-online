<section class="live-pane ${tab===id?'on':''}" data-pane="${id}">${html}</section>`;let statGrid=`<div class="compact-stats">${A.map(a=>`<button class="stat" data-a="stat" data-id="${c.id}" data-stat="${a}"><b>${a}</b><strong>${c.ds[a]}</strong><small>${sg(md(c.ds[a]))} • d20</small></button>`).join('')}</div>`;let summary=`<div class="sheet-section"><div class="sheet-kicker">ÖZET</div><h2>Oyun sırasında gerekenler</h2>${statGrid}</div><div class="sheet-grid2"><div class="sheet-info"><h3>❤️ Can</h3><div class="big">${c.hp_current} / ${c.hp}</div><div class="actions"><button class="small" data-a="hp" data-id="${c.id}" data-d="-5">−5</button><button class="small" data-a="hp" data-id="${c.id}" data-d="-1">−1</button><button class="small" data-a="hp" data-id="${c.id}" data-d="1">+1</button><button class="small" data-a="hp" data-id="${c.id}" data-d="5">+5</button></div></div><div class="sheet-info"><h3>🛡 Savunma & Hareket</h3><p><b>AC ${c.ac}</b> • Hız ${c.speed} ft • Seviye ${c.level}</p><p class="muted">${hv(c.species_name)} • ${hv(c.class_name)} • ${hv(c.background_name)}</p></div></div>`;let character=`<div class="sheet-section"><div class="sheet-kicker">KARAKTER</div><h2>${hv(c.name)}</h2><p><b>Irk:</b> ${hv(c.species_name)} • <b>Sınıf:</b> ${hv(c.class_name)} • <b>Arka Plan:</b> ${hv(c.background_name)}</p></div><div class="sheet-section"><h3>Statlar • zar atmak için tıkla</h3>${statGrid}</div><div class="sheet-grid2"><div class="sheet-info"><h3>Irk Bilgisi</h3><p>${hv(race.description||race.bonus_text||'')}</p></div><div class="sheet-info"><h3>Arka Plan</h3><p>${hv(bg.history||'')}</p><small class="muted">${hv(list(bg.skills).join(', ')||'')}</small></div></div>`;let weapons=`<div class="sheet-section"><div class="sheet-kicker">SİLAHLAR</div><h2>🎯 Vur • 💥 Hasar</h2><p class="muted">Vur yalnız saldırı zarını, Hasar yalnız hasar zarını atar. Sonuçlar GM Zar Akışı'na canlı gider.</p>${v14Inventory(c,'weapon')}</div>`;let items=`<div class="sheet-section"><div class="sheet-kicker">EŞYA & DURUM</div><h2>🎒 Envanter</h2>${v14Inventory(c,'gear')}</div>`;let powers=`${vampPanel(c)}${pathPanel(c)}<section class="card" style="margin:0"><div class="sheet-kicker">IRK GÜÇLERİ</div><div class="grid">${powerCards(c)}</div></section>`;let history=`<div class="sheet-section"><div class="sheet-kicker">ZAR GEÇMİŞİ</div>${rolls(mineRolls.slice(0,40))}</div>`;let party=pv&&pv.image_url?`<div class="sheet-info" style="margin-top:10px"><h3>🧭 GM Görseli</h3><img src="${hv(pv.image_url)}" alt="${hv(pv.title||'Parti görseli')}" style="display:block;width:100%;max-height:220px;object-fit:contain;border-radius:9px;background:#03070c"><b>${hv(pv.title||'Aktif Görsel')}</b></div>`:'';return`<div class="live-sheet"><section class="live-sheet-head"><div class="live-avatar">◇</div><div class="live-id"><div class="sheet-kicker">CANLI OYUNCU KAĞIDI</div><h1>${hv(c.name)}</h1><small>${hv(c.species_name)} • ${hv(c.class_name)} • ${hv(c.background_name)}</small></div><div class="live-mini"><div><span>HP</span><b>${c.hp_current}/${c.hp}</b></div><div><span>AC</span><b>${c.ac}</b></div><div><span>HIZ</span><b>${c.speed}</b></div><div><span>LV</span><b>${c.level}</b></div></div></section><div class="live-workspace"><section class="live-left"><nav class="live-tabs">${[['summary','🏠 Özet'],['character','🎲 Karakter'],['weapons','⚔️ Silahlar'],['items','🎒 Eşya & Durum'],['powers','✨ Güçler'],['history','🧾 Zar Geçmişi']].map(([id,l])=>`<button data-v14-sheet="${id}" data-id="${c.id}" class="${tab===id?'on':''}">${l}</button>`).join('')}</nav>${pane('summary',summary)}${pane('character',character)}${pane('weapons',weapons)}${pane('items',items)}${pane('powers',powers)}${pane('history',history)}</section><aside class="live-right"><div class="section-title"><div><div class="sheet-kicker">CANLI MASA</div><h2>Bağlantı & Zarlar</h2></div><span class="live">● CANLI</span></div><div class="quick-dice">${[4,6,8,10,12,20,100].map(d=>`<button data-v14-die="${d}" data-id="${c.id}">d${d}</button>`).join('')}</div><div class="last-result"><small class="muted">Son Zar</small><br><strong>${last?.total==null?'—':last.total}</strong>${last?`<div class="muted">${hv(last.label||last.roll_kind||'Zar')} • ${hv(last.formula||'')}</div>`:''}</div>${party}<div class="sheet-kicker" style="margin-top:10px">ZAR GÜNLÜĞÜ</div><div class="live-log">${rolls(mineRolls.slice(0,12))}</div></aside></div></div>`};
function rbDefaults(){let sp=st.speciesRows.map(x=>x.name),bg=st.backgroundRows.map(x=>x.name),cl=st.classRows.map(x=>x.name);window.__v14ReadyDraft=window.__v14ReadyDraft||{name:'',species:sp[0]||'',background:bg[0]||'',className:cl[0]||'',path:'',stats:{STR:15,DEX:14,CON:13,INT:12,WIS:10,CHA:8}};return window.__v14ReadyDraft}
function rbCapture(){let d=rbDefaults(),q=id=>document.querySelector(id);if(q('#rb-name'))d.name=q('#rb-name').value;if(q('#rb-species'))d.species=q('#rb-species').value;if(q('#rb-bg'))d.background=q('#rb-bg').value;if(q('#rb-class'))d.className=q('#rb-class').value;if(q('#rb-path'))d.path=q('#rb-path').value;A.forEach(a=>{if(q('#rb-'+a))d.stats[a]=q('#rb-'+a).value})}
function readyBuilderHTML(){let d=rbDefaults(),sp=st.speciesRows.map(x=>speciesInfo(x.name)),bg=st.backgroundRows.map(x=>bgInfo(x.name)),cl=st.classRows.map(x=>classInfo(x.name)),paths=st.paths.filter(x=>x.species_name===d.species);return`<section class="card ready-builder"><div class="section-title"><div><div class="eyebrow">GM • HAZIR KARAKTER</div><h2>Önceden karakter hazırla</h2><p class="muted">Karakter sahipsiz kaydolur; sonra Hazır Karakter Daveti ile oyuncuya verirsin.</p></div><button data-v14-ready="close">Kapat</button></div><div class="form"><label class="wide">Karakter adı<input id="rb-name" maxlength="60" value="${hv(d.name)}"></label><label>Irk<select id="rb-species">${sp.map(x=>`<option ${x.name===d.species?'selected':''}>${hv(x.name)}</option>`).join('')}</select></label><label>Arka plan<select id="rb-bg">${bg.map(x=>`<option ${x.name===d.background?'se